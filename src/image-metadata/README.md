# Image Metadata

This directory ties the runtime profile config to the bundled assets under
`src/runtime-images/` and `src/rollback-images/`.

Current metadata files:

- `runtime-bundle-index.json` for bundled guest payload directories
- `source-bundle-lock.json` for pinned git revisions of bundled source trees
