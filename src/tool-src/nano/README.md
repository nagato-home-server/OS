# nano Source Bundle Placeholder

Place the bundled `nano` source tree here.

Suggested contents:

- upstream release tarball or extracted source tree
- `VERSION`
- `SHA256SUMS`
- any HubOS-specific patch notes

The live editor binary can remain the host-installed `nano`, but the image can
carry the matching source bundle from this directory.
