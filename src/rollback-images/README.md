# Rollback Images

Keep known-good recovery payloads here.

Suggested structure:

```text
src/rollback-images/<profile>/<version>/
```

Populate this tree with immutable rollback artifacts once the first real guest
runtime images are staged.
